/// void scr_remove_self(ArgSet);
// This doesn't do anything, just has to exist.

remove_entity(id);

// No continue here, since the object with the cutscene index is now deleted

World.keep_going=false;
