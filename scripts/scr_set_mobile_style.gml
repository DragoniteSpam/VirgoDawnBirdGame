/// void scr_set_mobile_style(ArgSet);
/* sets interpolation style for the mobile translate entity.
 * Arguments:
 *   - InterpolationStyle style: the interpolation style to use
 */

style=argument0[| 0].value;

World.keep_going=true;
