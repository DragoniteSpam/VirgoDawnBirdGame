enum Stats {
    hp,
    atk,
    def,
    spa,
    spd,
    spe,
    acc,
    eva
}

stat_names[Stats.hp]=get_text(Text.statHP);
stat_names[Stats.atk]=get_text(Text.statAtk);
stat_names[Stats.def]=get_text(Text.statDef);
stat_names[Stats.spa]=get_text(Text.statSpA);
stat_names[Stats.spd]=get_text(Text.statSpD);
stat_names[Stats.spe]=get_text(Text.statSpE);
