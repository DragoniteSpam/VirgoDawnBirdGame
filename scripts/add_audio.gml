/// AudioGroup add_audio(internal name, name);

with (instantiate(AudioGroup, argument1)){
    internal_name=argument0;
    // if you want to add any other information do it here i guess
    return id;
}
