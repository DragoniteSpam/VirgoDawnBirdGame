/// void null(argument0..argumentn);
// Takes any number of arguments but doesn't do anything,
// good for when you might need a placeholder script

for (var i=0; i<argument_count; i++){
    var go_away=argument[i];
}

return false;
