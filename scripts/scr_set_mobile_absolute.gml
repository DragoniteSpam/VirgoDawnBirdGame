/// void scr_set_mobile_absolute(ArgSet);
/* sets the position of a keyframed path to be absolute or relative to the entity's origin.
 * Arguments:
 *   - boolean absolute: whether to use exact (true) or relative (false) coordinates in the map
 */

absolute=eval(argument0[| 0].value);

World.keep_going=true;
