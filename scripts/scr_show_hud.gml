/// void scr_show_hud(ArgSet);
/* Enables showing the game hud.
 * Arguments:
 *   - boolean show: true if you want to show the game HUD, false otherwise
 */

World.show_hud=argument0[| 0].value;

World.keep_going=true;
