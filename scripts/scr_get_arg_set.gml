/// ArgSet scr_get_arg_set();
// returns the argument set for the current location in the current
// cutscene.

//return ds_list_find_value(game_cutscene[World.cutscene.cutscene_index], World.cutscene.scene_index);

return 0;
