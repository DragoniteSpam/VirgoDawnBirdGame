/// boolean hit_open(type, attacker, strength);

var attacker=argument1;
var strength=argument2;

// This makes no sense
if (timer_invincibility<=0){
    if (argument0&(CombatTypes.unlock)){
        interact_treasure_chest();
    }
}

return false;
