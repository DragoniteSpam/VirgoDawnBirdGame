/// void scr_set_alive(ArgSet);
/* Makes an entity alive or not alive.
 * Arguments:
 *   - boolean alive: true if alive, false otherwise
 */

set_alive(argument0[| 0].value);

World.keep_going=true;
