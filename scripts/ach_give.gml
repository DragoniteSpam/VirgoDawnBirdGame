/// boolean ach_give(index);
// index should take the form of some enum value, like ach.a001menu

if (!ach_has(argument0)){
    achievement[argument0]=true;
    // @todo this
    //newAchievementGraphic(n);
}
