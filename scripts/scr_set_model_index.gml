/// void scr_set_model_index(ArgSet);
/* sets the model index for an Entity.
 * Arguments:
 *   - int modelIndex: the index of the model to change to
 */

model_index=argument0[| 0].value;

World.keep_going=true;
