/// void scr_remove_currency(ArgSet);
/* gives an item
 * Arguments:
 *   - double amount: the amount of currency to remove
 */

RemoveCurrency(argument0[| 0].value);

// @todo Play 'remove item' sound

World.keep_going=true;
