/// boolean qv_stub(Entity, param);

var entity=argument0;
var param=argument1;

// Invalid param - get rid of this if the stage does indeed accept a param
if (abs(param)>0){
    return false;
}

return false;
